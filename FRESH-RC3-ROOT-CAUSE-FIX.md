# FRESH RC3 — root-cause fix

Tracing found the failing QA text was generated by `skillCostStatusText()` in the main Development renderer at index.html, not by the quick-progress summary path changed in RC2.

RC3 changes only `skillCostStatusText()` so it directly constructs the full Development summary in EN/FR/DE/IT using the active `KOW_CLEAN_I18N` language.

No User Guide, dictionary, QA rule, or other application logic was changed.
